artworks show

  {{artwork.data.title}}

  {{artwork.data.medium}}

  {{artwork.data.date_seen}}
  <input type="text"
  ng-model="artwork.data.date_seen"
  ng-model-options='{ debounce: 1000 }'
  ng-change="updateDateSeen(artwork.data)"/>

  {{artwork.data.artist.name}}

  {{artwork.data.location}}

  <div ng-repeat="movement in artwork.data.movements">
    {{movement.name}}
  </div>
